/**
 * @author WMXPY
 * @namespace Compare
 * @description Compare
 */

import { CompareResult } from "./declare";

export const compareObject = (left: any, right: any): CompareResult[] => {

    return [];
};
